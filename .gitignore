# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
.vercel

# TypeScript build cache
tsconfig.tsbuildinfo

# Converted JK2 player models. Raven/Activision assets — this repo is public and
# git history is permanent, so they're deliberately kept out of it pending a
# decision on where they should be hosted. See docs/jk2-model-conversion.md and
# public/models/README.md. The CC0/CC-BY Khronos sample model is committed.
public/models/kyle.glb
